#include <stdio.h>

int main(void)
{
    int i = (5, 10);   // 10 is assigned to i

    while (i < 100)
        sum += data[i], ++i;

    int j = (f1(), f2());

    int x = (y = 3, (z = ++ y + 2) + 5);

    for (i=0, j=100; i!=10; ++i, j-=10)

    printf("jason"),
    printf("Fedin"),
    printf("Done");

    house_price = 249,500; // same as
    house_price = 249;
    500;


    house_price = (249, 500);    // house_price is 500

    char ch, date;
    int a = 1, b = 2;
    printf("%d:%d", x, y);

    void fun(x, y);

    return 0;
}
